language: cpp
sudo: false

compiler :
  - gcc
  - clang

addons:
  apt:
    sources:
      - debian-stretch
    packages:
      - cppcheck
      - clang
      - libprotobuf-dev
      - binutils
      - iwyu

script:
  - mkdir build
  - cd build
  - cmake -DCHECK_CODE=1 ..
  - make
  - make check

branches:
  only:
    - master

os:
  - linux
