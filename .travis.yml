language: cpp
sudo: false

compiler :
  - gcc
  - clang

addons:
  apt:
    sources:
      - alias: debian-stretch
      - sourceline: 'deb http://ftp.us.debian.org/debian stretch main'
    packages:
      - cppcheck
      - clang
      - libprotobuf-dev
      - binutils
      - iwyu

script:
  - mkdir build
  - cd build
  - cmake -DCHECK_CODE=1 ..
  - make
  - make check

branches:
  only:
    - master

os:
  - linux
