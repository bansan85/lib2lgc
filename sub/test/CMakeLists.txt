include(FindProtobuf)
find_package(Protobuf REQUIRED)

include_directories(${CMAKE_CURRENT_SOURCE_DIR}/../inc)

if (INCLUDE_POCO)
        include_directories(${INCLUDE_POCO})
endif()
if (LIBRARY_POCO)
        link_directories(${LIBRARY_POCO})
endif()
if (INCLUDE_POCO_PROTOBUF)
        include_directories(${INCLUDE_POCO_PROTOBUF})
endif()
if (INCLUDE_HEADER)
        include_directories(${INCLUDE_HEADER})
endif()
if (INCLUDE_MATH)
        include_directories(${INCLUDE_MATH})
endif()
if (LIBRARY_MATH)
        link_directories(${LIBRARY_MATH})
endif()

add_executable(visitor1 EXCLUDE_FROM_ALL ${PROTO_HEADER} ${PROTO_SRC} visitor1.cc)
target_link_libraries(visitor1 ${Protobuf_LIBRARIES} 2lgcpoco 2lgcmath)
add_test(visitor1 visitor1)

add_dependencies(check visitor1)

